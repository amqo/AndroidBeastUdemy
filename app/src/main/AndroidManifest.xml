<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alberto.beastmainproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".infrastructure.BeastApplication" >

        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.BrotherPagerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomDialogActivity"/>

        <activity android:name=".activities.PhotoPagerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/CustomDialogActivity"/>

        <activity android:name=".activities.YoutubeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/YoutubeDialogActivity"/>

        <activity android:name=".activities.ActivityMaps"
            android:screenOrientation="portrait"/>

        <activity android:name=".activities.ActivityMapsCampus"
            android:screenOrientation="portrait"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAVeXl6aSyFOv62k_ST-nVviMtfZRZby8w" />
    </application>

</manifest>